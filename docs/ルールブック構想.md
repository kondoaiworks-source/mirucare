# ルールブック構想（運営向け IA の正）

この文書は、運営向け「ルール設定」の **中心概念とメニュー構成** を固定する。  
実装・画面文言はここに矛盾しないこと。

最終更新: 2026-08-14

関連憲章：[プロダクト完成図.md](./プロダクト完成図.md)  
Phase1 確定：[PHASE1_REDESIGN.md](./PHASE1_REDESIGN.md)

---

## 1. ひとことでいうと

**サービス・領域・自治体を選ぶと下書きのルールブックが出る。人が直して確定したものが物差しになる。**  
施設が上げた書類は、書類同士の整合と、このルールブックとの整合を確認する。

本線:

1. 介護サービスと領域と対象自治体を選ぶ  
2. 下書きのルールブックが自動で出る  
3. 人が **修正・追加・削除** して **確定** する  
4. 書類チェックは、書類同士＋確定済みルールブックとの整合を見る  

根拠URLの登録・監視は裏方（生成の材料・更新検知）。人がURLを1件ずつセットしてからルールを作る作業は、本線にしない。

**「全て」は領域マスタの1件ではない。** 運用中の領域をまとめて選ぶ仮想選択肢。

---

## 2. メニュー構成（正）

トップ名称：**ルール設定**  
サイドナビは **利用設定／監視状況** の2本のみ。

```
ルール設定
├─ 利用設定
│    ├─ サービス設定
│    │    ├─ 訪問介護
│    │    │    ├─ ルールブックを作る
│    │    │    ├─ 国・県設定
│    │    │    └─ 自治体設定
│    │    ├─ 通所介護（準備中）
│    │    └─ その他（準備中）
│    └─ 領域マスタ
└─ 監視状況
```

**サマリは置かない。** 進め方カードや横断ショートカットも置かない。

国・県設定／自治体設定の根拠URL・判定ルール管理は、裏方の保守画面として残す。

---

## 3. 動線ルール（迷子防止）

1. **1画面1作業**（例：3つ選んで下書きを作る／下書きを直して確定する／領域マスタ）
2. **横飛びリンク禁止**（監視状況・判定ルール・国県などを別枠から飛ばさない）
3. **次の作業は親へ戻ってから進む**（パンくずが正）
4. **説明文は必要最小限**（枠の見出し＋動詞リンクで足りる）
5. **各作業は明確な枠（カード／section）で区切る**

---

## 4. 用語の正（UIに出す言葉）

| 正 | 意味 | UIに出さない |
|----|------|----------------|
| サービス設定 | 訪問介護などの一覧 | — |
| 領域マスタ | チェック領域の追加・修正・停止・削除 | カテゴリ |
| 領域 | 人員基準・勤務表・加算・減算・請求要件など | audit category |
| 全て | 運用中領域のまとめ選択（マスタ行ではない） | — |
| ルールブックを作る | 3軸から下書きを出す入口 | ルールセット |
| ルールブック下書き | 確定前の一式 | ジョブ |
| 確定する | 下書きを本番の物差しにする | 一括承認 |
| 国・県設定 | サービス共通の根拠URLと共通ルール（裏方） | — |
| 自治体設定 | 市の公開と市配下の作業入口（裏方） | ルールセット |
| 登録自治体 | 公開／停止する市の一覧 | — |
| 自治体マスタ | 管轄のマスタ一覧 | — |
| 根拠URL設定 | PDF等の登録・修正・削除 | 公開情報（監視詳細以外） |
| 判定ルール管理 | 承認待ちと登録済みの管理 | AI判定ルール |
| 承認待ち | 本番前の確認キュー | 了承待ち（旧） |
| 登録ルール一覧 | 利用中・登録済みのルール | — |
| ルールブック | 確定済み判定ルール一式（サービス×領域×市） | ルールセット |
| 監視状況 | 根拠URLの同期結果 | 監視トラブル |

内部の **ルールセット／audit_items** は画面に出さない。市名は「横浜市」のように出す。

---

## 5. 画面対応

| 構想上の名前 | 場所 |
|--------------|------|
| 利用設定 | `/admin/rules/setup` |
| 領域マスタ | `/admin/rules/domains` |
| サービス（訪問介護） | `/admin/rules/services/homecare` |
| ルールブックを作る | `.../compose` |
| ルールブック下書き | `.../compose/[jobId]` |
| 国・県設定 | `.../national-prefecture` |
| 国・県の判定ルール管理 | `.../national-prefecture/rules` |
| 自治体設定 | `.../municipalities` |
| 登録自治体（市） | `.../municipalities/[city]` |
| 市の判定ルール管理 | `.../municipalities/[city]/rules` |
| 手動で生成 | 各判定ルール管理の `/manual` |

旧全市ページ（`/admin/rules/pending`・`/admin/rules/manual`）は利用設定へリダイレクトする。

チェック時の足し方：**国・県で確定した共通ルール ＋ その市で確定したルール**（例：横浜＝共通＋横浜市）。

| 監視状況 | `/admin/rules/monitoring` |

旧「カテゴリ設定」（`/admin/rules/audit-items`）は訪問介護ハブへリダイレクトする。

---

## 6. 領域マスタ

初期領域（slug は変えない。停止は可。削除は不可）:

- 人員基準（`staffing`）
- 勤務表（`shift-table`）
- 加算・減算（`addition-reduction`）
- 請求要件（`billing`）

運営は領域の **追加・修正・停止・削除** ができる。

- 停止：新規の生成・選択から外す。既存ルールブックは残す
- 削除：初期領域以外、かつ判定ルールが紐づいていないときだけ

---

## 7. 安全装置（変えないこと）

1. URLを読んだだけで本番チェック基準にしない  
2. 下書きの自動生成 ≠ 確定（人が確定するまでチェックに使わない）  
3. 旧版を消し飛ばさない  
4. 断定禁止・Wチェック支援の憲章は維持  

---

## 8. Phase1 の範囲

- サービス：**訪問介護**（通所等は準備中）  
- 自治体：**横浜・川崎・藤沢・鎌倉・茅ヶ崎**（＋国・神奈川県）  
- 領域：上記4つ（＋全て）。追加は領域マスタから  

---

*2026-08-14：本線をサービス×領域×自治体の下書き→確定へ。領域マスタを追加。根拠URL作業は裏方。*  
*2026-08-12：判定ルールの生成・承認・修正・削除は国・県または各市の画面で完結。全市ページを廃止。コードは内部自動採番。*
