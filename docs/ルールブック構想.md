# ルールブック構想（運営向け IA の正）

この文書は、運営向け「ルール設定」の **中心概念とメニュー構成** を固定する。  
実装・画面文言はここに矛盾しないこと。細かい操作手順は [チェック設定ホーム操作マニュアル.md](./チェック設定ホーム操作マニュアル.md)（名称は順次「ルール設定」へ寄せる）。

最終更新: 2026-08-09

関連憲章：[プロダクト完成図.md](./プロダクト完成図.md)  
Phase1 確定：[PHASE1_REDESIGN.md](./PHASE1_REDESIGN.md)

---

## 1. ひとことでいうと（ユーザーが覚えること）

**このサービス・この自治体で運営するなら、このルールブックに従えばよい。**

- ルールブックは **サービス単位** で、国＋県＋市の公開情報を束ねた「確定版の従うべき基準」
- 運営メニューは **利用設定** と **監視状況** の2本
- **利用設定**で根拠URLと判定ルールを整え、**監視状況**で同期エラーを見る
- AIが **判定ルール案＋根拠** を出し、**人が了承**したものだけが最新の確定版になる
- 書類チェックは、この確定版（の中の承認済み判定ルール）を参照する
- 施設（管理者）は、運営が **運用中** にしたサービス×自治体を選んで利用開始する（当面は訪問介護×5市・1組）

### 更新ループ（目指す動き）

```
利用設定
  → 提供サービスを選ぶ（例：訪問介護）
  → 国・県／市の根拠URLを登録
  → 判定ルールを生成（API）または手入力 → 了承
監視状況
  → 国・自治体ごとの監視リスト
  → エラー印から詳細確認・差分／再同期
```

※ URLを読んだだけで本番チェックに載せない。了承前は提案のみ。  
※ **一覧ページURLだけの登録だと、台帳には載っても「以降の変更を自動監視」までは行かないことがある。更新アラートにはPDF直リンクが必要。**

---

## 2. メニュー構成（目標）

トップ名称：**ルール設定**  
入口：`/admin/rules` → `/admin/rules/setup`（利用設定）

```
ルール設定
├─ 利用設定（入口）
│    ├─ 登録サマリ
│    ├─ 提供サービス
│    │    └─ [サービス]（例：訪問介護）
│    │         ├─ 国・県の根拠URL
│    │         └─ 市区町村（公開／停止・市の根拠URL・監査カテゴリ）
│    ├─ サービス×自治体（根拠URL／判定ルールへの導線）
│    └─ 判定ルール（生成・手入力・了承・履歴）
└─ 監視状況
     ├─ 監視サマリ（エラー／差分／正常）
     └─ 国・県・市の監視リスト → 詳細
```

**判定ルールの生成と了承**は利用設定内の「ルール管理」。市・国県画面は根拠URLの登録に集中する。

---

## 3. いまの画面との対応（移行マップ）

| 構想上の名前 | いま開く場所 | 状態 |
|--------------|--------------|------|
| ルール設定入口 | `/admin/rules` → `/admin/rules/setup` | **利用設定** |
| 利用設定 | `/admin/rules/setup` | **正** |
| 監視状況 | `/admin/rules/monitoring` | **正** |
| サービス詳細 | `/admin/rules/services/[service]` | **正**（利用設定から） |
| 国・県の根拠URL | `/admin/rules/services/[service]/national-prefecture` | **正** |
| 市区町村 | `/admin/rules/services/[service]/municipalities` | **正** |
| 市の公開情報 | `.../municipalities/[city]` | **正** |
| 監査カテゴリ | `.../[city]/audit-categories` | **正** |
| ルール管理 | `/admin/rules/pending` | 利用設定配下 |
| 旧介護サービス選定一覧 | `/admin/rules/services` | → 利用設定へリダイレクト |
| 旧監視トラブル | `/admin/rules/more` | → 監視状況へリダイレクト |
| 公開情報監視 | `/admin/rules/documents` | 監視状況から |
| マニュアル差分 | `/admin/document-changes` | 監視状況から |

---

## 4. 安全装置（変えないこと）

1. **URLを読んだだけで本番チェック基準にしない**  
2. **台帳への反映 ≠ 判定ルールの自動反映**（人が通したものだけ）  
3. **旧版を消し飛ばさない**（いつの基準で見たか説明可能）  
4. 断定禁止・Wチェック支援の憲章は維持

---

## 5. Phase1 の範囲

- サービス：**訪問介護**（通所等は停止＝準備中）
- 自治体（市）：**横浜・川崎・藤沢・鎌倉・茅ヶ崎**（＋国・神奈川県の層）
- 施設選択：サービス×自治体は **当面1組**（複数組合せは将来）
- 国・県PDF：**サービスごとに共通**（市をまたいで共有）
- 監査カテゴリ：現行4（ケアプラン⇔計画書／計画⇔実績／シフト⇔提供記録／国保連請求⇔実績）。**将来追加可能**
- 了承：カテゴリ内の状況表示＋横断「ルール管理」（**生成の主導線もルール管理**）
- 関連PDFの自動検索→採用／不採用：国・県・市の登録済み公開情報からキーワード検索し、候補を人が採用／不採用。採用で台帳監視開始（外部サイトの新規クロールは今後）

---

## 6. 施設向けとの関係

- 施設はオンボーディング／設定で、運営が **運用中** にしたサービス×自治体から選ぶ  
- **ルールブック本体の編集・承認は運営**が行う  
- 施設はチェック結果で「このチェックで使った基準」を見る

---

*2026-08-09：メニューを「利用設定／監視状況」の2本に再編。*
